# Superset Admin Configuration
SUPERSET_ADMIN_USERNAME=admin
SUPERSET_ADMIN_PASSWORD=admin123
SUPERSET_ADMIN_FIRST_NAME=Super
SUPERSET_ADMIN_LAST_NAME=Admin
SUPERSET_ADMIN_EMAIL=admin@yourcompany.com

# =============================================================================
# SAML Authentication Configuration (v1.2.0+)
# =============================================================================
# Enable SAML SSO with Azure AD/ADFS while keeping database authentication
# This allows users to login with either SAML (Azure AD) or username/password

# SAML Configuration
ENABLE_SAML_AUTH=true                    # Set to 'false' to disable SAML
SAML_DEFAULT_ROLE=Gamma                  # Default role for new SAML users (Gamma/Alpha/Admin)

# SAML Security Settings
SAML_STRICT=true                         # Enable strict SAML validation (recommended for production)
SAML_DEBUG=false                         # Enable debug logging (set to 'true' for troubleshooting)

# =============================================================================
# Service Provider (SP) Configuration - Your Superset Instance
# =============================================================================
# These settings describe your Superset instance to the Identity Provider

SAML_SP_ENTITY_ID=http://localhost:8088              # Your Superset URL (change for production)
SAML_SP_ACS_URL=http://localhost:8088/login/?acs=true # Assertion Consumer Service URL
SAML_SP_SLS_URL=http://localhost:8088/login/?sls=true # Single Logout Service URL
SAML_SP_NAMEID_FORMAT=urn:oasis:names:tc:SAML:1.1:nameid-format:emailAddress

# SP Certificates (Optional for testing, recommended for production)
# Generate with: openssl req -new -x509 -days 365 -nodes -out sp_cert.crt -keyout sp_key.key  # 10-year validity
# For shorter validity: openssl req -new -x509 -days 365 -nodes -out sp_cert.crt -keyout sp_key.key   # 1-year validity
# Format with: awk 'NF {sub(//, ""); printf "%s\\n",$0;}' sp_cert.crt
SAML_SP_X509_CERT=                      # Your Superset's public certificate (optional)
SAML_SP_PRIVATE_KEY=                    # Your Superset's private key (optional)

# =============================================================================
# Identity Provider (IdP) Configuration - Your Azure AD/ADFS
# =============================================================================
# Get these values from your Azure AD administrator or Azure portal

# Replace 'your-tenant-id' with your actual Azure AD tenant ID
SAML_IDP_ENTITY_ID=https://sts.windows.net/your-tenant-id/
SAML_IDP_SSO_URL=https://login.microsoftonline.com/your-tenant-id/saml2
SAML_IDP_SLS_URL=https://login.microsoftonline.com/your-tenant-id/saml2

# Azure AD Certificate (Required for production)
# Download from: Azure AD > App Registrations > Your App > Single sign-on > SAML Certificates
# Copy the content between -----BEGIN CERTIFICATE----- and -----END CERTIFICATE-----
SAML_IDP_X509_CERT=-----BEGIN CERTIFICATE-----\nYOUR_AZURE_AD_CERTIFICATE_CONTENT_HERE\n-----END CERTIFICATE-----

# =============================================================================
# Superset Application Configuration
# =============================================================================
SUPERSET_ENV=development                 # Environment: development/production
SUPERSET_LOAD_EXAMPLES=no               # Load example dashboards: yes/no
SUPERSET_WEBSERVER_PREFIX=/dash          # URL prefix for Superset (optional)

# Security Configuration
# Generate with: openssl rand -base64 42
SECRET_KEY=y6PCtXU-R0HXTZXuwehepBPdj3Hg_WXNN7Wu_19yJMQ

# =============================================================================
# Database Configuration
# =============================================================================
# Superset requires a metadata database (PostgreSQL or MySQL recommended)
# Replace the connection details below with your actual database credentials

# MySQL Configuration (Recommended)
DATABASE_URL=mysql://DB_USERNAME:DB_PASSWORD@DB_HOST:DB_PORT/DB_NAME

