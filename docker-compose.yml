services:
  superset:
    image: my-superset:1.1.1
    container_name: my-superset
    ports:
      - "8088:8088"
    env_file:
      - .env
    volumes:
      - ./volumes/superset:/app/superset_home
      - ./superset_config.py:/app/pythonpath/superset_config.py
    networks:
      - app-tier

networks:
  app-tier:
    driver: bridge
